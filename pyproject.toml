[project]
name = "pulsometer"
version = "0.1.0"
description = "Real-time pulse measurement from webcam using rPPG (CHROM)"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.4.2",
    "opencv-python>=4.13.0.92",
    "scipy>=1.17.1",
]
